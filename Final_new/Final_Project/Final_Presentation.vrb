\frametitle{Proposed Model Architecture - Feed Forward}

  \begin{verbatim}
    def forward(self, x):
        x = self.flatten(x)
        logits = self.linear_relu_stack(x)
        return logits
  \end{verbatim}

